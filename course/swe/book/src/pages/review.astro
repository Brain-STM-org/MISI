---
/**
 * Review Session Page
 * Dedicated interface for reviewing due concepts using spaced repetition
 */
import BookLayout from '../layouts/BookLayout.astro';
import { ReviewSession } from '@repo/book-viewer/components';
---

<BookLayout title="Review Session">
  <div class="max-w-2xl mx-auto">
    <header class="text-center mb-8">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">
        Review Session
      </h1>
      <p class="text-gray-600 dark:text-gray-400">
        Strengthen your memory by reviewing key concepts at optimal intervals
      </p>
    </header>

    <ReviewSession client:only="react" />

    <div class="mt-12 p-6 bg-gray-50 dark:bg-gray-800 rounded-xl">
      <h2 class="font-semibold text-gray-900 dark:text-white mb-3">
        How Spaced Repetition Works
      </h2>
      <div class="space-y-3 text-sm text-gray-600 dark:text-gray-400">
        <p>
          When you first review a concept, you'll see it again in <strong>1 day</strong>.
          Each successful review extends the interval:
        </p>
        <div class="flex flex-wrap gap-2">
          <span class="px-3 py-1 bg-purple-100 dark:bg-purple-900/50 text-purple-700 dark:text-purple-300 rounded-full text-xs">
            1 day
          </span>
          <span class="text-gray-400">→</span>
          <span class="px-3 py-1 bg-purple-100 dark:bg-purple-900/50 text-purple-700 dark:text-purple-300 rounded-full text-xs">
            3 days
          </span>
          <span class="text-gray-400">→</span>
          <span class="px-3 py-1 bg-purple-100 dark:bg-purple-900/50 text-purple-700 dark:text-purple-300 rounded-full text-xs">
            7 days
          </span>
          <span class="text-gray-400">→</span>
          <span class="px-3 py-1 bg-purple-100 dark:bg-purple-900/50 text-purple-700 dark:text-purple-300 rounded-full text-xs">
            14 days
          </span>
          <span class="text-gray-400">→</span>
          <span class="px-3 py-1 bg-purple-100 dark:bg-purple-900/50 text-purple-700 dark:text-purple-300 rounded-full text-xs">
            30 days
          </span>
        </div>
        <p>
          This schedule is based on the <strong>Ebbinghaus forgetting curve</strong> —
          reviewing just before you forget maximizes retention with minimal effort.
        </p>
      </div>
    </div>
  </div>
</BookLayout>
